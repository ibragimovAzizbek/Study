import 'package:flutter/material.dart';

class Screen3Page extends StatelessWidget {
  const Screen3Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      key: const ValueKey('bodys'),
    );
  }
}
